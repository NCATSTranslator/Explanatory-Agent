apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ea.fullname" . }}-test-connection"
  labels:
    {{- include "ea.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ea.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
